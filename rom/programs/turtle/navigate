local navigator = loader.require.navigator

local tArgs = { ... }
if #tArgs ~= 3 then
  print( "Usage: navigate <x> <y> <z>" )
  return
end

local x = tonumber(tArgs[1])
local y = tonumber(tArgs[2])
local z = tonumber(tArgs[3])

navigator.find(vector.new(x, y, z))
